"use client";

import React from "react";
import { 
  LuUserPlus, 
  LuShieldCheck, 
  LuActivity, 
  LuMoveHorizontal, 
  LuMail, 
  LuPhone,
  LuCircle 
} from "react-icons/lu";

export default function StaffPage() {
  const staffMembers = [
    { id: 1, name: "Jean-Pierre", role: "Administrateur", status: "Actif", sales: "1,2M", email: "jp@pos.com" },
    { id: 2, name: "Marie-Louise", role: "Caissière Senior", status: "Actif", sales: "850K", email: "ml@pos.com" },
    { id: 3, name: "Abdoulaye", role: "Caissier", status: "Absent", sales: "420K", email: "ab@pos.com" },
  ];

  return (
    <div className="min-h-screen bg-white text-zinc-900 font-sans">
      
      {/* HEADER : TITRE & ACTION */}
      <header className="px-10 py-12 border-b-2 border-zinc-900 flex justify-between items-end">
        <div>
          <div className="flex items-center gap-3 mb-2">
            <h1 className="text-4xl font-black uppercase tracking-tighter italic">Personnel</h1>
            <span className="bg-zinc-100 text-[10px] font-black px-3 py-1 rounded-full uppercase tracking-widest">
                {staffMembers.length} Membres
            </span>
          </div>
          <p className="text-zinc-400 font-medium uppercase text-xs tracking-[0.2em]">Gestion des accès et suivi des performances</p>
        </div>
        
        <button className="flex items-center gap-3 bg-zinc-900 text-white px-8 py-4 font-black uppercase tracking-widest text-xs hover:bg-zinc-800 transition-all shadow-[6px_6px_0px_0px_rgba(24,24,27,0.2)] active:translate-x-1 active:translate-y-1 active:shadow-none">
          <LuUserPlus className="w-4 h-4" /> Ajouter un membre
        </button>
      </header>

      <div className="p-10 max-w-400 mx-auto">
        
        {/* SECTION : STATS RAPIDES */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
          <div className="border-2 border-zinc-900 p-8 flex justify-between items-start">
            <div>
                <p className="text-xs font-black uppercase tracking-widest text-zinc-400 mb-4">Ventes Équipe (Mois)</p>
                <p className="text-4xl font-mono font-black italic">2,470,000 <span className="text-sm not-italic opacity-30">F</span></p>
            </div>
            <LuActivity className="w-6 h-6 text-zinc-900" />
          </div>
          <div className="border-2 border-zinc-900 p-8 flex justify-between items-start">
            <div>
                <p className="text-xs font-black uppercase tracking-widest text-zinc-400 mb-4">Taux de présence</p>
                <p className="text-4xl font-mono font-black italic text-emerald-600">94%</p>
            </div>
            <LuCircle className="w-6 h-6 fill-emerald-600 text-emerald-600" />
          </div>
          <div className="border-2 border-zinc-900 p-8 bg-zinc-900 text-white flex justify-between items-start">
            <div>
                <p className="text-xs font-black uppercase tracking-widest text-zinc-500 mb-4">Droits d'accès</p>
                <p className="text-2xl font-black uppercase tracking-tighter">Sécurité : Élevée</p>
            </div>
            <LuShieldCheck className="w-6 h-6" />
          </div>
        </div>

        {/* SECTION : GRILLE DU PERSONNEL */}
        <h2 className="text-xs font-black uppercase tracking-[0.3em] text-zinc-400 mb-8">Équipe active</h2>
        <div className="grid grid-cols-1 xl:grid-cols-2 gap-6">
          {staffMembers.map((member) => (
            <div key={member.id} className="group border-2 border-zinc-100 p-8 flex items-center justify-between hover:border-zinc-900 transition-all">
              <div className="flex items-center gap-8">
                {/* Avatar Minimaliste */}
                <div className="w-20 h-20 bg-zinc-100 border-2 border-zinc-900 flex items-center justify-center relative">
                    <span className="text-2xl font-black uppercase">{member.name.substring(0,2)}</span>
                    <div className={`absolute -bottom-1 -right-1 w-4 h-4 border-2 border-white rounded-full ${member.status === 'Actif' ? 'bg-emerald-500' : 'bg-rose-500'}`} />
                </div>

                <div className="space-y-1">
                  <h3 className="text-2xl font-black uppercase tracking-tighter">{member.name}</h3>
                  <p className="text-xs font-black uppercase tracking-widest text-zinc-400">{member.role}</p>
                  <div className="flex gap-4 pt-2">
                    <span className="text-[10px] font-mono font-bold flex items-center gap-1 text-zinc-500"><LuMail className="w-3 h-3"/> {member.email}</span>
                  </div>
                </div>
              </div>

              <div className="text-right flex flex-col items-end gap-4">
                <div className="text-right">
                    <p className="text-[10px] font-black uppercase tracking-widest text-zinc-300">Ventes totales</p>
                    <p className="text-2xl font-mono font-black italic">{member.sales} F</p>
                </div>
                <button className="p-2 hover:bg-zinc-100 rounded-full transition-colors">
                  <LuMoveHorizontal className="w-6 h-6" />
                </button>
              </div>
            </div>
          ))}
        </div>

      </div>

      {/* FOOTER DISCRET */}
      <footer className="p-10 border-t border-zinc-100 mt-12 flex justify-between text-[10px] font-black uppercase tracking-[0.3em] text-zinc-300">
        <span>© 2026 Système de Gestion Terminal</span>
        <span className="text-zinc-900">Logs de connexion : 11:45:02</span>
      </footer>
    </div>
  );
}